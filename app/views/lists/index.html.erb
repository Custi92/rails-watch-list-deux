<div class="lists-container">
  <h1>Voici vos listes:</h1>
  <% @lists.each do |list| %>
    <div class="list-card">
      <%= link_to list.nom, list_path(list), class: "text-decoration-none" %>
    </div>
  <% end %>
</div>
